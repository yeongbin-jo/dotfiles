# showcase / meta files — kept in the repo, never deployed to $HOME
README.md
Brewfile
iterm2.json
macos.sh

# ── role=server (eq/WSL 등 헤드리스): tmux 설정만 scoped, 나머지 dotfiles 제외 ──
#    → prod 에 age 키/zsh·vim 설정 안 내려보냄. .tmux.conf + 플러그인 설치 스크립트만 적용.
{{- if eq .role "server" }}
.claude
.codex
.config
.gitconfig
.screenrc
.vimrc
.zshrc
.zshrc.local
{{- /* age 키 없는 server 는 .age 접미사를 못 떼므로 양쪽 타겟명(.sh / .sh.age) 모두 무시 */}}
.tmux/window-name-alias.sh*
{{- end }}

# 파이썬 캐시 — 스킬 스크립트를 import 하면 생기는데 dotfile 이 아니다
.claude/skills/**/__pycache__

# 저장소 자체 테스트 — $HOME 에 배포하지 않는다
tests
tests/**
