interface:
  display_name: "Claude Artifact Publish"
  short_description: "Create, revise, QA, and publish Claude Artifacts"
  default_prompt: "Use $claude-artifact-publish to create or revise a Claude Artifact, preserve its agreed content and design boundaries, save and verify it, and share it publicly only when explicitly requested."
